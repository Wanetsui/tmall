<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tmall.mapper.InformationMapper">
  <resultMap id="BaseResultMap" type="tmall.pojo.Information">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="title" jdbcType="VARCHAR" property="title" />
    <result column="info" jdbcType="VARCHAR" property="info" />
    <result column="time" jdbcType="TIMESTAMP" property="time" />
    <result column="catalog" jdbcType="INTEGER" property="catalog" />
  </resultMap>


  <!--根据主键查询一条用户数据 -->
  <select id="selectByPrimaryKey" resultMap="BaseResultMap"
          parameterType="java.lang.Integer">
    select
    id,title,info,createTime
    from information
    where id = #{id,jdbcType=INTEGER}
  </select>
  <select id="findByPage" parameterType="Map" resultMap="BaseResultMap">
    select
    id,title,info,createTime
    from information
      limit #{start},#{size}
  </select>
  <!--查询所有用户数据 -->
  <select id="selectInformationList" resultType="tmall.pojo.Information">
    select
    *
    from information
  </select>
  <!-- 查询用户记录总数 -->
  <select id="selectCount" resultType="int">
    select count(*) from information
  </select>
  <insert id="insert" parameterType="tmall.pojo.Information" keyColumn="id" keyProperty="id" useGeneratedKeys="true">
    INSERT  INTO information(title,info,catalog) VALUES (#{title},#{info},#{catalog})
  </insert>
  <delete id="delete">
    DELETE from information WHERE id = #{id}
  </delete>
  <update id="edit">
    UPDATE information set title = #{title} ,info = #{info} where id = #{id}
  </update>
  <select id="searchByKeyword" resultMap="BaseResultMap">
    SELECT * from information where title like concat('%',#{keyword},'%')
  </select>
  <select id="getInformationsByCatalog" resultMap="BaseResultMap">
    SELECT * FROM information WHERE catalog = #{catalog}
  </select>
</mapper>